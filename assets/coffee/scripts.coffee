$ ->
	$('.marquee').marquee
		gap: 0,
		delayBeforeStart: 0,
		startVisible: true,
		duplicated: true
	# .on 'mouseenter', () ->
	# 	$(this).marquee('pause')
	# .on 'mouseleave', (e) ->
	# 	$(this).marquee('resume')
